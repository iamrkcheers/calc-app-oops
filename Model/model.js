var object={
    add:function(fno,sno){return parseInt(fno)+parseInt(sno);},
    subtract:function(fno,sno){return parseInt(fno)-parseInt(sno);},
    divide:function(fno,sno){return parseInt(fno)/parseInt(sno);},
    multiply:function(fno,sno){return parseInt(fno)*parseInt(sno);},
}